# Skill Contract

| 项目 | 约定 |
|---|---|
| 使用的 Skill 与版本 | `skill-creator`；`douyin-benchmark-video-monitor` v0.3.1（Windows 单入口） |
| 用户目标 | 核对自动采集链路可运行，并确保视频先下载到本地，再使用本机 FFmpeg 与 Whisper 转写视频文案；发现问题时完成优化并发布到 GitHub。 |
| 输入与唯一真源 | 私有 `config/project.local.json` 中的对标账号、关键词、飞书配置；Codex 内置浏览器中的用户登录态；浏览器采集产物 `codex-browser-candidates.json` 与 `codex-browser-summary.json`。 |
| 固定母版/不可修改项 | 仅保留 Windows 安装与运行入口；对标账号串行采集；账号窗口 30 天；关键词窗口 7 天；关键词最多检查 10 条并最多入榜 5 条；四个固定飞书视图及用户指定字段顺序。 |
| 禁止项 | 不上传账号、关键词、飞书 ID、凭据、登录态、采集结果、缓存或本地绝对路径；不并发采集对标账号；不绕过验证码；不将空视频文案批次写入飞书；不写入用户受保护的私人素材目录。 |
| 语义与素材规则 | 每条榜单视频优先使用浏览器捕获的可播放地址；否则由 yt-dlp 从公开视频页解析。视频必须下载到本地临时目录，由本地 FFmpeg 提取音频并由本地 faster-whisper 转写；中文结果转为简体。 |
| 样片/确认闸门 | 使用本机生成的中文语音测试视频完成端到端内部样片；验证直接本地文件、HTTP 下载、缓存复用和全失败拦截。该内部测试无需等待用户确认。 |
| 完整必交产物 | 可安装的 Windows Skill 仓库；四视图字段配置；依赖自动安装脚本；本地转写脚本；运行编排脚本；自动化测试；验收清单；GitHub 更新。 |
| 技术与人工 QA | Node/Python 测试、PowerShell 语法、Python 编译、Skill 校验、真实本地转写、HTTP 下载转写、缓存复用、全失败阻断、完整运行状态、隐私扫描、`git diff --check`。 |
| 归档位置 | 工程与中间测试产物位于当前工作区；测试中间文件位于仓库已忽略的 `work/`；公开交付同步到用户指定的 GitHub 仓库。 |
